// jest.setup.js
// Optional: Global setup for Jest tests
// For example, you can mock global objects or set up a test database connection here.

// Example: Mocking Next.js specific functionalities if needed by your tests globally
// jest.mock('next/router', () => require('next-router-mock'));
// jest.mock('next/config', () => () => ({ publicRuntimeConfig: {}, serverRuntimeConfig: {} }));

// You can also extend Jest's expect matchers here if needed
// import '@testing-library/jest-dom'; // if using testing-library

console.log('Jest global setup file loaded.');
